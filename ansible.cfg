[defaults]
# workstation.yml defaults to the machine it's run on, so an implicit localhost
# is the normal case here rather than something to warn about.
localhost_warning = False

[inventory]
inventory_unparsed_warning = False
