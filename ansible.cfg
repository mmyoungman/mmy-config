[defaults]
# This playbook only ever targets the machine it's run on, so there's no
# inventory to parse and nothing to warn about.
localhost_warning = False

[inventory]
inventory_unparsed_warning = False
