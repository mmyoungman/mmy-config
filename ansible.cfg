[defaults]
# Both playbooks default to the machine they're run on, so an implicit
# localhost is the normal case rather than something to warn about.
# server.yml can also be pushed at inventory.ini with -i and -e target=.
localhost_warning = False

[inventory]
inventory_unparsed_warning = False
